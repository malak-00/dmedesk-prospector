// Cloudflare Pages version of frontend-config.js.
// The only difference from vercel/frontend-config.js: VERCEL_API_URL is the
// full production URL because CF Pages is cross-origin to the Vercel API.
// The vercel/ copy keeps VERCEL_API_URL = "" (same-origin, no CORS needed).

const VERCEL_API_URL = "https://dmedesk-prospector.vercel.app";
